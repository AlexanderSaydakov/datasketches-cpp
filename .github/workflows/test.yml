name: SerDe Test

on:
  workflow_dispatch:

jobs:
  build:
    name: SerDe Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Checkout Java
        uses: actions/checkout@v2
        with:
          repository: apache/datasketches-java
          path: java
      - name: Run Java
        run: cd java && mvn test
      - name: Run cmake
        run: cd build && cmake ..
      - name: Build C++ unit tests
        run: cmake --build build --config Release
      - name: Run C++ tests
        run: cmake --build build --config Release --target test
      - name: List files
        run: find .
#      - name: Upload artifacts
#        uses: actions/upload-artifact@v3
#        with:
#          name: sketch
#          path: build/kll/test/kll_double_estimation_cpp.sk
